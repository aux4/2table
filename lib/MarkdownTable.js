import { AsciiTable } from './AsciiTable.js';

/**
 * MarkdownTable for compatibility with existing interface
 */
export class MarkdownTable extends AsciiTable {
  // For now, just use ASCII output
  // Can be extended later for proper Markdown formatting
}